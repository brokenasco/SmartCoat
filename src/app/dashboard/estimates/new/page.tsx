import Link from "next/link"; import { redirect } from "next/navigation"; import { EstimateBuilder } from "@/components/estimate-builder"; import { createClient } from "@/lib/supabase/server"; import { getCompanyEntitlement } from "@/lib/entitlements";
export default async function NewEstimate(){const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user)redirect("/login"); const {data:m}=await supabase.from("company_memberships").select("company_id").eq("user_id",user.id).eq("status","active").limit(1).single(); if(!m)redirect("/dashboard"); const {hasPremiumAccess}=await getCompanyEntitlement(supabase,m.company_id); if(!hasPremiumAccess)redirect("/dashboard"); return <main className="min-h-screen"><header className="border-b border-border bg-surface"><div className="mx-auto max-w-7xl px-5 py-4"><Link href="/dashboard" className="text-sm font-semibold text-brand">← Dashboard</Link></div></header><div className="mx-auto max-w-7xl px-5 py-8"><p className="text-sm text-muted">Draft estimate</p><h1 className="mb-7 text-3xl font-semibold">Build a room estimate</h1><EstimateBuilder companyId={m.company_id}/></div></main>}
