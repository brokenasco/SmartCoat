import Link from "next/link"; import { AuthForm } from "@/components/auth-form"; import { hasSupabaseEnv } from "@/lib/env";
export default async function Login({ searchParams }: { searchParams: Promise<{mode?:string}> }) { const {mode}=await searchParams; return <main className="grid min-h-screen place-items-center px-6 py-12"><div className="w-full max-w-md"><Link href="/" className="text-xl font-bold"><span className="text-brand">Smart</span>Coat</Link><div className="mt-8 rounded-2xl border border-border bg-surface p-7 shadow-sm"><h1 className="text-2xl font-semibold">{mode==="signup"?"Build a more profitable company":"Welcome back"}</h1><p className="mb-7 mt-2 text-sm text-muted">Secure access to your company workspace.</p><AuthForm initialSignup={mode==="signup"} configured={hasSupabaseEnv()}/></div></div></main> }
