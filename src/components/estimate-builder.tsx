"use client";
import { useMemo, useState } from "react";
import { calculateEstimate } from "@/lib/domain/estimate-engine";
import { getEstimatedRetailerPrice } from "@/lib/domain/retailer-pricing";
import { formatMoney } from "@/lib/domain/pricing";
import { createClient } from "@/lib/supabase/browser";

export function EstimateBuilder({companyId}:{companyId:string}){
 const [form,setForm]=useState({name:"",length:15,width:12,height:8,window1Width:3,window1Height:4,window2Width:4,window2Height:5,coats:2,coverage:400,waste:10,workers:2,wage:2500,burden:20,productionRate:150,prepHours:2,overhead:10,margin:45,zip:"33601",retailer:"home_depot" as "home_depot"|"lowes"}); const [status,setStatus]=useState("");
 const pricing=useMemo(()=>getEstimatedRetailerPrice(form.retailer,/^\d{5}$/.test(form.zip)?form.zip:"00000"),[form.retailer,form.zip]);
 const result=useMemo(()=>calculateEstimate({room:{lengthFeet:form.length,widthFeet:form.width,heightFeet:form.height},openings:[{kind:"window",widthFeet:form.window1Width,heightFeet:form.window1Height},{kind:"window",widthFeet:form.window2Width,heightFeet:form.window2Height}],coats:form.coats,coverageSqFtPerGallon:form.coverage,wastePercent:form.waste,paintPricePerGallonCents:pricing.pricePerGallonCents,productionRateSqFtPerHour:form.productionRate,prepHours:form.prepHours,crewSize:form.workers,averageWageCentsPerHour:form.wage,laborBurdenPercent:form.burden,overheadPercent:form.overhead,targetGrossMarginPercent:form.margin,productiveHoursPerDay:8,retailer:form.retailer,zipCode:/^\d{5}$/.test(form.zip)?form.zip:"00000",pricingSource:pricing.source,pricingTimestamp:pricing.updatedAt}),[form,pricing]);
 function set(name:keyof typeof form,value:string){setForm(x=>({...x,[name]:["name","zip","retailer"].includes(name)?value:Number(value)} as typeof form))}
 async function save(){if(!form.name.trim())return setStatus("Give this estimate a name first.");if(!/^\d{5}$/.test(form.zip))return setStatus("Enter a valid five-digit ZIP code.");setStatus("Saving…");const supabase=createClient();const {data,error}=await supabase.from("estimates").insert({company_id:companyId,title:form.name,status:"draft",subtotal_cents:result.customerSubtotalCents,tax_cents:result.taxCents,total_cents:result.customerEstimateCents,cost_cents:result.totalContractorCostCents,target_margin_percent:form.margin,calculation_snapshot:{formulaVersion:result.formulaVersion,inputs:form,result}}).select("id").single();setStatus(error?error.message:`Draft ${data.id.slice(0,8)} saved.`)}
 const field=(label:string,name:keyof typeof form,step="1")=><label className="text-sm font-medium">{label}<input value={form[name]} onChange={e=>set(name,e.target.value)} type="number" min="0" step={step} className="mt-1 min-h-11 w-full rounded-lg border border-border px-3 font-mono"/></label>;
 return <div className="grid gap-6 xl:grid-cols-[1fr_380px]"><section className="space-y-6 rounded-xl border border-border bg-surface p-6"><div><h2 className="font-semibold">Estimate details</h2><p className="text-sm text-muted">Formula {result.formulaVersion} keeps takeoff, labor, duration, and pricing consistent everywhere.</p></div><label className="block text-sm font-medium">Estimate name<input value={form.name} onChange={e=>set("name",e.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-border px-3"/></label><div className="grid gap-4 sm:grid-cols-3">{field("Length (ft)","length",".01")}{field("Width (ft)","width",".01")}{field("Height (ft)","height",".01")}{field("Coats","coats")}{field("Coverage (ft²/gal)","coverage")}{field("Waste %","waste",".1")}</div><div className="grid gap-4 sm:grid-cols-4">{field("Window 1 width","window1Width",".01")}{field("Window 1 height","window1Height",".01")}{field("Window 2 width","window2Width",".01")}{field("Window 2 height","window2Height",".01")}</div><div className="rounded-lg bg-background p-4"><dl className="grid grid-cols-2 gap-3 text-sm sm:grid-cols-4"><Metric l="Gross walls" v={`${result.grossSurfaceAreaSqFt} ft²`}/><Metric l="Openings" v={`−${result.deductedOpeningAreaSqFt} ft²`}/><Metric l="Net walls" v={`${result.netPaintableAreaSqFt} ft²`}/><Metric l="Paint" v={`${result.rawGallonsRequired} → ${result.recommendedGallons} gal`}/></dl></div><div className="grid gap-4 sm:grid-cols-3">{field("Workers","workers")}{field("Average wage (¢/hr)","wage")}{field("Labor burden %","burden",".1")}{field("Production ft²/hr","productionRate",".1")}{field("Prep hours","prepHours",".25")}{field("Overhead %","overhead",".1")}</div><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Retailer<select value={form.retailer} onChange={e=>set("retailer",e.target.value)} className="mt-1 min-h-11 w-full rounded-lg border border-border px-3"><option value="home_depot">Home Depot</option><option value="lowes">Lowe&apos;s</option></select></label><label className="text-sm font-medium">ZIP code<input value={form.zip} inputMode="numeric" maxLength={5} onChange={e=>set("zip",e.target.value.replace(/\D/g,"").slice(0,5))} className="mt-1 min-h-11 w-full rounded-lg border border-border px-3"/></label></div></section><aside className="h-fit rounded-xl bg-[#16251d] p-6 text-white"><label className="block text-sm">Target gross margin <span className="float-right font-mono">{form.margin}%</span><input aria-label="Target gross margin" type="range" min="0" max="70" value={form.margin} onChange={e=>set("margin",e.target.value)} className="mt-3 w-full accent-emerald-400"/></label><dl className="mt-7 space-y-3 border-y border-white/10 py-5 text-sm"><Row l="Materials (estimated)" v={formatMoney(result.materialSubtotalCents)}/><Row l="Wages" v={formatMoney(result.wageCostCents)}/><Row l="Labor burden" v={formatMoney(result.laborBurdenCents)}/><Row l="Overhead" v={formatMoney(result.overheadCents)}/><Row l="Contractor cost" v={formatMoney(result.totalContractorCostCents)}/><Row l="Expected gross profit" v={formatMoney(result.expectedGrossProfitCents)}/></dl><p className="mt-5 text-xs text-emerald-100/60">Recommended customer price</p><p className="font-mono text-3xl">{formatMoney(result.customerEstimateCents)}</p><p className="mt-2 text-xs text-emerald-100/70">{result.laborHours} labor hours · {result.estimatedWorkingDays} working days with {result.crewSize} workers</p><p className="mt-4 text-xs text-amber-200">{result.warnings.join(" ")}</p><button onClick={save} className="mt-6 min-h-12 w-full rounded-lg bg-emerald-400 font-semibold text-emerald-950">Save draft</button>{status&&<p role="status" className="mt-3 text-sm">{status}</p>}</aside></div>
}
function Metric({l,v}:{l:string,v:string}){return <div><dt className="text-muted">{l}</dt><dd className="font-mono font-semibold">{v}</dd></div>}
function Row({l,v}:{l:string,v:string}){return <div className="flex justify-between gap-4"><dt>{l}</dt><dd className="font-mono">{v}</dd></div>}
